import Header from '../Header';
import HeroSection from '../HeroSection';

const Home = () => {
    return (
        <div className="relative">
            <HeroSection />
            <Header />
        </div>
    );
}

export default Home;